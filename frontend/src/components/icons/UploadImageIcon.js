import React from 'react';
import theme from 'theme';

/**
 * Upload image icon
 *
 * @param {string} width
 * @param {string} color
 */
export const UploadImageIcon = ({ width, color }) => {

  return (
    <img style={{width: 18}} src={require('assets/images/image-generic-icon-gray.png')} />
  );
};
